import React from 'react';
import spinner from '../../Image/Bean Eater.gif';

const Spinner = () => {
    return (
        <section className="vh-100 vw-100 d-flex justify-content-center align-items-center">
        <img src={spinner} alt=""/>
        
    </section>
    );
};

export default Spinner;